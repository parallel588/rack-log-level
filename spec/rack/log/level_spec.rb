require 'spec_helper'

describe Rack::Log::Level do
  it 'has a version number' do
    expect(Rack::Log::Level::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
